<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simulation Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            background-color: #f4f4f9;
            display: flex;
            flex-direction: column;
            min-height: 100vh;
        }

        header {
            background-color: #333;
            color: #fff;
            padding: 15px 20px;
            position: fixed;
            top: 0;
            width: 100%;
            z-index: 1000;
        }

        .navbar {
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .navbar-brand {
            font-size: 1.8em;
            font-weight: bold;
        }

        main {
            flex: 1;
            margin-top: 80px; /* space for fixed header */
            display: flex;
            justify-content: center;
            align-items: center;
            padding: 40px 20px;
        }

        .card-container {
            display: flex;
            gap: 30px;
            flex-wrap: wrap;
            justify-content: center;
        }

        .nav-card {
            background-color: #fff;
            border: 1px solid #ddd;
            border-radius: 12px;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
            width: 220px;
            height: 150px;
            text-align: center;
            display: flex;
            justify-content: center;
            align-items: center;
            text-decoration: none;
            color: #333;
            transition: transform 0.2s, box-shadow 0.2s;
            font-size: 1.2em;
            font-weight: bold;
        }

        .nav-card:hover {
            transform: scale(1.05);
            box-shadow: 0 8px 16px rgba(0, 128, 0, 0.2);
            background-color: #e6f8ec;
            color: #2e7d32;
        }

        footer {
            text-align: center;
            padding: 10px;
            background-color: #eee;
            margin-top: auto;
        }
    </style>
</head>
<body>

    <header>
        <nav class="navbar">
            <div class="navbar-brand">Simulation Page</div>
        </nav>
    </header>

    <main>
        <div class="card-container">
            <a href="/armcontro" class="nav-card">LPC2148 Arm Controller</a>
            <a href="/aurdino" class="nav-card">LPC2148 Arduino Controller</a>
            <!-- Add more cards as needed -->
        </div>
    </main>

    <footer>
        &copy; 2025 Simulation Page. All rights reserved.
    </footer>

    <script>
        window.onload = function() {
            history.pushState(null, null, document.URL);
        };

        window.addEventListener('popstate', function () {
            history.pushState(null, null, document.URL);
        });
    </script>
</body>
</html>
